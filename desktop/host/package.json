{
  "name": "consulting-toolkit-desktop-host",
  "version": "0.2.0",
  "private": true,
  "type": "commonjs",
  "description": "Desktop host for consulting-toolkit: HTTP + token guards + static assets + PDF. Owns the Playwright dependency.",
  "main": "app.js",
  "engines": {
    "node": ">=25.0.0"
  },
  "dependencies": {
    "consulting-toolkit": "file:../../packages/core",
    "playwright": "^1.56.1"
  },
  "scripts": {
    "install-browser": "npx playwright install chromium"
  }
}
